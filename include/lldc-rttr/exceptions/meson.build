exceptions_header_dir = join_paths(install_header_dir, 'exceptions')

headers = [
  'exceptions.h'
]

install_headers(headers, install_dir: exceptions_header_dir)
unset_variable('exceptions_header_dir')
